using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace ConsoleApplication3
{
    class Program
    {
        static void Main(string[] args)
        {
            /*
            Q3.guessLetters("THUPHEHEY");

            Q2 q2 = new Q2();
            StringAuditQuestion.Pair[] pairs=q2.auditString("aaaadaaabbdddbbeeeexxxxxxddddderererddfd");
            foreach (StringAuditQuestion.Pair p in pairs)
            {
                Console.WriteLine(p._char + " " + p._occurances);

            }
            byte[] test = { (byte)0x02, (byte)0x67, (byte)0x67, (byte)0x83, (byte)0xFF,(byte)0x83, (byte)0xFF, (byte)0x00};
            byte[] output;
            Q1.DecompressRLE(test, out output);
            Console.WriteLine();
            for (int i = 0; i < output.Length; i++)
            {
                Console.WriteLine(output[i]);
            }*/
           
        }

    }
}
